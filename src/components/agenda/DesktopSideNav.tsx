/**
 * DesktopSideNav — DEPRECATED.
 *
 * The desktop side nav was removed in favor of an always-horizontal bottom nav
 * (see AgendaBottomNav). This stub is kept only so any lingering imports
 * elsewhere (e.g. tests, snapshots) don't break at compile time. It renders
 * nothing and is not referenced by AgendaShell.
 */

export function DesktopSideNav() {
  return null;
}
